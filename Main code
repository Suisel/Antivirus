import java.util.Scanner;


public class BorsheVirus {
	
	
	/**
	 * prints default code
	 */
	private static void printHelloPage(){
		System.out.println("This it Borsh Anti-virus");
		System.out.println("type 'exit' to exit the program");
		System.out.println("type 'add signature' to add a signature");
		System.out.println("type 'view developers' to see developers list");
		System.out.println("type 'check file' to check a file");
		System.out.println("type 'add directory' to add a new directory to Hash base");
		System.out.println("type 'check directories' to check directories");
	}
	/**
	 * used to print developer's names
	 */
	private static void printDevelopers(){
		System.out.println("Leader - Rita");
		System.out.println("Press any key to continue");
	}
	
	static Scanner scan = new Scanner(System.in);
	private static String readStringIn(){
		return scan.nextLine();
	}
	
	static FileChecker file;
	static SignatureData base;
	public static void main(String args[]){
		base = new SignatureData("SignatureData.txt");;
		boolean stop = false;
		String string;
		FileData fileData = new FileData();
		while (!stop){
			printHelloPage();
			string = readStringIn();
			if (string.equals("exit")){
				stop = true;
			}
			
			if (string.equals("view developers")){
				printDevelopers();
				readStringIn();
			}
			
			if (string.equals("add signature")){
				System.out.println("Enter signature then press enter");
				string = readStringIn();
				base.add(string);
				System.out.println("Added!");
			}
			
			if (string.equals("check file")){
				System.out.println("Enter file name");
				string = readStringIn();
				file = new FileChecker(string, base);
			}
			
			if (string.equals("add directory")){
				System.out.println("Enter directory path (c:\\Windows as example)");
				string = readStringIn();
				fileData.addToFileData(string);
			}
			
			if (string.equals("check directories")){
				System.out.println("Checking files...");
				fileData.check();
			}
			
			
			
		}
		base.close();
		fileData.close();
	}
}
